<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css" />
  <link rel="stylesheet" href="/css/headerStyles.css" />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      background-color: #fff;
    }

    .hero-section {
      display: flex;
      flex-direction: column;
      min-height: calc(100vh - 140px); /* header + footer space */
    }

    .hero-image {
      flex: 1;
      background: url('/smart.png') center/cover no-repeat;
      min-height: 50vh;
      position: relative;
    }

    .hero-image::after {
      content: '';
      position: absolute;
      bottom: 0;
      width: 100%;
      height: 80px;
      background: linear-gradient(to bottom, rgba(255,255,255,0) 0%, #f0f8ff 100%);
    }

    .quote-section {
      flex: 1;
      background: linear-gradient(to bottom right, #f0f8ff, #e6f7ff);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 3rem 1rem;
      position: relative;
      overflow: hidden;
    }

    .quote-box {
      max-width: 850px;
      background-color: rgba(255, 255, 255, 0.6);
      padding: 2rem;
      border-radius: 20px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      font-style: italic;
      color: #003344;
      font-size: 1.8rem;
      font-weight: 500;
      line-height: 1.6;
      text-align: center;
      position: relative;
      animation: fadeInUp 1s ease-in-out;
    }

    .quote-box::before {
      content: "“";
      font-size: 5rem;
      position: absolute;
      top: -20px;
      left: 30px;
      color: #00aaff;
      opacity: 0.3;
    }

    .quote-box::after {
      content: "”";
      font-size: 5rem;
      position: absolute;
      bottom: -30px;
      right: 30px;
      color: #00aaff;
      opacity: 0.3;
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 768px) {
      .quote-box {
        font-size: 1.3rem;
        padding: 1.5rem;
      }
    }
  </style>
</head>

<body>
  <%- include('partials/header') %>

  <section class="hero-section">
    <div class="hero-image"></div>

   
  </section>

  <%- include('partials/footer') %>

  <script src="/js/script.js"></script>
</body>
</html>
